import BlogContent from '@/components/blog/BlogContent';

export const metadata = {
  title: 'Expedition Logbook | Luxor',
  description: 'Field notes, luxury route guides, and raw adventure logs from the edge of the world.',
};

export default function BlogPage() {
  return <BlogContent />;
}
