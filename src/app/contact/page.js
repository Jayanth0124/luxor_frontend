import ContactContent from '@/components/contact/ContactContent';

export const metadata = {
  title: 'Contact Us | Luxor',
  description: 'Get in touch with the Luxor concierge team for premium expedition bookings, vehicle rentals, and remote campsite stays.',
};

export default function ContactPage() {
  return <ContactContent />;
}
